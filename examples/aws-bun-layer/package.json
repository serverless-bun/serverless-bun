{
  "name": "aws-bun-layer",
  "devDependencies": {
    "@serverless-bun/layer": "0.1.0",
    "@types/bun": "^1.0.4",
    "serverless": "^3.38.0"
  },
  "scripts": {
    "sls": "sls",
    "build": "bun build ./src/handler.ts --outdir=dist",
    "predeploy": "bun run build",
    "deploy": "sls deploy"
  },
  "type": "module",
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
